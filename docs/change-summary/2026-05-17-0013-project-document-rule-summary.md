# 项目文档规则改动总结

## 1. 改动目标

本次改动为当前项目新增项目级协作规则，明确以后每次代码改动后都要同步生成 Markdown 总结文档，避免功能实现完成后缺少设计说明、实现说明和验证记录。

## 2. 用户视角

后续每次项目功能发生变化后，用户都可以在 `docs/change-summary/` 中查看对应总结文档，了解某个功能为什么实现、如何实现、涉及哪些文件、如何运行验证。例如登录页面的实现方式、后端登录接口、数据库用户表和 Token 保存流程都应在对应总结中说明。

## 3. 前端实现

本次没有修改前端业务页面和组件，只在项目根目录补充文档规则。后续前端功能改动时，总结文档需要说明页面、组件、路由、状态管理、接口请求和样式布局。

## 4. 后端实现

本次没有修改后端 Java 代码。后续后端功能改动时，总结文档需要说明 Controller、Service、Mapper、Entity、DTO、VO、配置项和异常处理逻辑。

## 5. 数据库与数据流

本次没有修改数据库结构。后续涉及数据库的改动，需要在总结中说明数据从前端输入、后端处理、数据库保存、再返回前端展示的完整链路。

## 6. 接口说明

本次没有新增或修改接口。

| 接口 | 方法 | 作用 | 主要参数 | 返回结果 |
|---|---|---|---|---|
| 无 | 无 | 本次仅新增文档规则 | 无 | 无 |

## 7. 关键文件

| 文件 | 作用 |
|---|---|
| `AGENTS.md` | 当前项目级规则，规定文档目录、日志目录、源码目录边界和每次代码改动后的总结文档要求 |
| `docs/change-summary/README.md` | 说明代码改动总结目录的用途、命名格式和内容要求 |
| `README.md` | 同步补充项目文档沉淀规则，提示总结文档存放位置 |
| `docs/change-summary/2026-05-17-0013-project-document-rule-summary.md` | 本次文档规则变更的总结文档 |

## 8. 验证方式

本次为文档规则改动，未运行构建命令。已确认新增文档位于项目根目录和 `docs/change-summary/` 目录下。

## 9. 风险与后续优化

当前规则依赖执行者自觉在每次代码改动后生成总结文档。后续可以继续增加脚本或 Git Hook，在提交前检查 `docs/change-summary/` 是否存在对应改动总结。
