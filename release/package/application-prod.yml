spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

app:
  jwt-secret: ${JWT_SECRET}
  jwt-expire-hours: ${JWT_EXPIRE_HOURS:24}
  upload-dir: ${UPLOAD_DIR:uploads/exam-files}

logging:
  level:
    com.example.olineaqspring: WARN
